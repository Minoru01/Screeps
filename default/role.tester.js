//var djikstra = require('Konsole');

var roleTester = {
    run: function(creep) {
        var targets = creep.room;
        console.log(targets);
    //djikstraAlgorithm.run();
        
    }
};
module.exports = roleTester;